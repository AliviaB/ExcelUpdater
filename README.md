# ExcelUpdater
Create a python code which will read a json file and a pre populated excel file. The excel file will have column a populated with table attributes which will be mapped against json file to find the key with hierarchy like data.team.memberId from an attribute memberId
